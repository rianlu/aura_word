import 'package:flutter/material.dart';

class AppColors {
  // 品牌核心色 (活力蓝)
  static const Color primary = Color(0xFF257BF4);
  // 品牌辅助色 (阳光黄 - 用于复习与强调)
  static const Color secondary = Color(0xFFFFC107);
  // 点缀色 (时尚紫 - 呼应启动页)
  static const Color tertiary = Color(0xFF7C3AED);
  
  // 背景体系 (极致通透)
  static const Color background = Color(0xFFF9FBFF); // 极淡蓝白，提升清爽感
  static const Color backgroundDark = Color(0xFF0F172A);
  static const Color surface = Colors.white;
  static const Color surfaceVariant = Color(0xFFF1F5F9); // 浅灰蓝，用于占位

  // 文本体系
  static const Color textHighEmphasis = Color(0xFF0F172A); // 深蓝黑
  static const Color textMediumEmphasis = Color(0xFF475569); // 蓝灰色，提升阅读感
  static const Color textLowEmphasis = Color(0xFF94A3B8);

  // 功能性柔和色 (用于卡片填充或非选中态)
  static const Color primaryLight = Color(0xFFEFF6FF);
  static const Color secondaryLight = Color(0xFFFFFBEB);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // 阴影体系
  static const Color shadowBlue = Color(0xFF1A5DBD);
  static const Color shadowYellow = Color(0xFFD4AA00);
  static const Color shadowWhite = Color(0xFFE2E8F0);
}
